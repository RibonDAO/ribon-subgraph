{
  "name": "subgraphribon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ribondao/subgraphribon",
    "test": "graph test",
    "coverage": "graph test -- -c",
    "create-local": "graph create --node http://localhost:8020/ ribondao/subgraphribon",
    "remove-local": "graph remove --node http://localhost:8020/ ribondao/subgraphribon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ribondao/subgraphribon"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.26.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
